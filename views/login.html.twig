{% extends "layout_adminLTE.html.twig" %}
{% set active = 'login' %}
{% set title = 'Login'|trans %}

{% block content %}
    <!--div class="row">
        <div class="col-lg-6">
            <h2>{{ title }}</h2>

            <form action="{{ path('admin_login_check') }}" method="post">
                {{ error }}
                <input type="text" description="_username" value="{{ last_username }}">
                <input type="password" description="_password" value="">
                <input type="submit">
            </form>
        </div>
    </div-->


    <div class="container login-form">
        <h2>{{ title }}</h2>
        <div class="row">
          <div class="col-sm-6 col-sm-offset-3 col-md-4 col-md-offset-4 col-lg-4 col-lg-offset-4">
              <form action="{{ path('admin_login_check') }}" method="post" class="form-signin" role="form">
                {{ error }}
                <input type="hidden" name="acceso" value="1" />
                <label class="text">{{ 'Username'|trans  }}:</label>
                <input type="text" class="form-control" placeholder="{{ 'Username'|trans  }}" name="login" description="_username" id="login" value="{{ last_username }}"" required autofocus>
                <label class="password">{{ 'Password'|trans  }}:</label>
                <input type="password" class="form-control" placeholder="{{ 'Password'|trans }}" required name="clave" description="_password" value="" id="clave">
                <label class="checkbox">
                  <input type="checkbox" value="remember-me"> Recordarme.
                </label>
                <input type="submit" class="btn btn-lg btn-primary btn-block" value="Acceder" />
              </form>
          </div>
      </div>

    </div>
{% endblock %}